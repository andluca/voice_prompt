#!/bin/bash
# View the project specification

cat << 'EOF'
📋 SPEC.md - Project Specification
================================================================================

View the complete specification:
  cat SPEC.md

Or view specific sections:
  grep -A 20 "## Core Requirements" SPEC.md
  grep -A 30 "## Architecture" SPEC.md
  grep -A 20 "## Technical Stack" SPEC.md

Key sections:
  - Core Requirements: What the tool must do
  - Architecture: Component structure and data flow
  - Technical Stack: Libraries and dependencies
  - Configuration: config.yaml structure
  - File Structure: Project organization
  - Testing Strategy: How to test components
  - Performance Optimization: Speed and memory targets

EOF

# Open SPEC.md in less for easy browsing
if [ -f "SPEC.md" ]; then
    echo "Press 'q' to exit, '/' to search, 'n' for next match"
    echo "================================================================================"
    less SPEC.md
else
    echo "❌ SPEC.md not found in current directory"
fi
